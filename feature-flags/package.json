{
  "name": "feature-flags-gray-release",
  "private": true,
  "version": "0.1.0",
  "description": "A small, extensible feature flag and gradual rollout service.",
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "npm run build --workspace @feature-flags/api && npm run build --workspace @feature-flags/web",
    "test": "npm run test --workspace @feature-flags/api",
    "typecheck": "npm run typecheck --workspace @feature-flags/api && npm run typecheck --workspace @feature-flags/web",
    "start": "npm run start --workspace @feature-flags/api"
  },
  "engines": {
    "node": ">=20"
  }
}
